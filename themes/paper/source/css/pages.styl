// pages — archive / tag / category / about

// ---- Shared page header ----
.page-head
  max-width var(--content-width)
  margin 0 auto var(--sp-12)
  text-align left

.page-head h1
  font-family var(--font-serif)
  font-size clamp(2.2rem, 6vw, 4rem)
  font-weight 500
  color var(--text-heading)
  letter-spacing 0
  margin 0 0 var(--sp-2)

.page-head .page-sub
  color var(--text-muted)
  font-size 0.95rem
  letter-spacing 0

// ---- Archive / timeline ----
.archive
  max-width var(--content-width)
  margin 0 auto

.archive-year
  margin var(--sp-12) 0 var(--sp-5)
  font-family var(--font-serif)
  font-size 1.25rem
  font-weight 500
  color var(--text-heading)
  letter-spacing 0

.archive-year:first-child
  margin-top 0

.archive-row
  display flex
  align-items baseline
  gap var(--sp-5)
  padding var(--sp-2) 0
  color var(--text-main)
  font-size 15px

.archive-date
  font-variant-numeric tabular-nums
  color var(--text-muted)
  font-size 13px
  min-width 56px
  flex-shrink 0

.archive-title
  flex 1
  color var(--text-main)
  overflow hidden
  text-overflow ellipsis
  white-space nowrap
  transition color var(--transition-base)

.archive-title:hover
  color var(--accent-color)

// ---- Category index (book-index style) ----
// Clean single-column list. Hover: page floats slightly right with
// a paper-white surface revealing underneath — like turning a page.
.category-index
  max-width 600px
  margin 0 auto
  display flex
  flex-direction column
  gap var(--sp-2)

.category-index-item
  display flex
  justify-content space-between
  align-items center
  padding var(--sp-4) var(--sp-6)
  border-radius var(--radius-md)
  color var(--text-main)
  background transparent
  transition background-color var(--transition-base), transform var(--transition-base)

.category-index-item:hover
  background var(--surface-color)
  transform translateX(6px)

.category-index-item .name
  font-size 1.05rem
  font-weight 500

.category-index-item .count
  color var(--text-subtle)
  font-family var(--font-mono)
  font-size 0.9rem
  font-variant-numeric tabular-nums

.category-index-item .count::before
  content '('
.category-index-item .count::after
  content ')'

// ---- Tag cloud (naked monochrome — air + typography, zero background) ----
.tag-cloud-naked
  display flex
  flex-wrap wrap
  gap 1.5rem 2.5rem
  justify-content flex-start
  max-width 800px
  margin 3rem auto

.tag-item-naked
  display inline-flex
  align-items baseline
  padding 0
  background transparent
  border none
  text-decoration none
  position relative
  transition all 0.3s cubic-bezier(0.4, 0, 0.2, 1)

.tag-item-naked .tag-prefix
  color var(--text-subtle)
  font-size 0.85rem
  margin-right 0.15rem
  font-weight 300

.tag-item-naked .tag-name
  color var(--text-main)
  font-size 1.05rem
  font-weight 400
  letter-spacing 0.02em

.tag-item-naked .tag-count
  color var(--text-subtle)
  font-size 0.75rem
  margin-left 0.25rem
  font-weight 300

.tag-item-naked:hover .tag-name
  color var(--accent-color)

.tag-item-naked:hover .tag-prefix,
.tag-item-naked:hover .tag-count
  color var(--text-muted)

.tag-item-naked::after
  content ''
  position absolute
  bottom -4px
  left 0
  width 0%
  height 1px
  background-color var(--accent-color)
  transition width 0.3s ease

.tag-item-naked:hover::after
  width 100%

// ---- Single taxonomy (tag/category) — post list ----
.taxonomy-posts
  max-width var(--content-width)
  margin 0 auto

.taxonomy-posts .archive-row
  border-bottom 1px solid var(--border-color)

.taxonomy-posts .archive-row:last-child
  border-bottom none

// ---- About page ----
.about
  max-width var(--content-width)
  margin 0 auto
